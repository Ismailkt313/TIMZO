<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    .navbar {
      background-color: #1a1a1a;
      padding: 8px 0;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
    }

    .navbar-brand img {
      height: 28px;
      /* Slightly smaller logo */
      transition: transform 0.3s ease;
    }

    .navbar-brand img:hover {
      transform: scale(1.05);
    }

    .navbar-nav {
      margin: 0 auto;
    }

    .navbar-nav .nav-link {
      color: #ffffff !important;
      font-weight: 500;
      font-size: 0.9rem;
      /* Smaller font size */
      text-transform: uppercase;
      margin: 0 10px;
      padding: 4px 8px;
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    .navbar-nav .nav-link:hover {
      color: #ffffff !important;
      background-color: #ffd700;
      border-radius: 5px;
    }

    .navbar-nav .nav-link.active {
      background-color: #6f42c1;
      color: #ffffff !important;
      border-radius: 5px;
    }

      .sidebar {
      height: 100%;
      width: 0;
      position: fixed;
      z-index: 1050;
      top: 0;
      left: 0;
      background-color: var(--dark-bg);
      overflow-x: hidden;
      transition: 0.3s;
      padding-top: 60px;
    }


    .sidebar a {
      padding: 10px 30px;
      text-decoration: none;
      font-size: 16px;
      color: var(--muted);
      display: block;
      transition: 0.3s;
      text-transform: uppercase;
    }
    .sidebar a:hover {
      background-color: var(--hover-bg);
      color: var(--highlight);
    }


    .sidebar .closebtn {
      position: absolute;
      top: 10px;
      right: 25px;
      font-size: 30px;
      color: #adb5bd;
    }

    .sidebar .closebtn:hover {
      color: #ffffff;
    }

    .openbtn {
      font-size: 20px;
      /* Smaller button */
      cursor: pointer;
      background-color: transparent;
      color: #ffffff;
      border: none;
      padding: 8px;
      transition: color 0.3s ease;
    }

    .openbtn:hover {
      color: #ffd700;
    }

    .search-container .form-control {
      background-color: transparent;
      border: 1px solid #adb5bd;
      color: #ffffff;
      border-radius: 20px;
      padding: 4px 12px;
      width: 180px;
      /* Slightly smaller input */
      font-size: 0.85rem;
    }

    .search-container .form-control::placeholder {
      color: #adb5bd;
    }

    .search-container .form-control:focus {
      background-color: transparent;
      border-color: #ffd700;
      color: #ffffff;
      box-shadow: none;
    }

    .navbar .btn-link {
      color: #ffffff;
      font-size: 0.9rem;
      transition: color 0.3s ease;
    }

    .navbar .btn-link:hover {
      color: #ffd700;
    }

    .badge {
      background-color: #ffd700 !important;
      color: #1a1a1a !important;
      font-size: 0.75rem;
    }

    .dropdown-menu {
      background-color: #1a1a1a;
      border: none;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
    }

    .dropdown-menu .dropdown-item {
      color: #adb5bd;
      font-size: 0.85rem;
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    .dropdown-menu .dropdown-item:hover {
      background-color: #2c2c2c;
      color: #ffd700;
    }

    .auth-buttons .btn {
      color: #ffffff;
      font-weight: 500;
      font-size: 0.9rem;
      padding: 4px 12px;
      transition: color 0.3s ease;
    }

    .auth-buttons .btn:hover {
      color: #ffd700;
    }

    .right-section {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .user-name {
      color: #ffffff;
      font-weight: 500;
      font-size: 0.9rem;
    }

    .user-name:hover {
      color: #ffd700;
    }

    @media (max-width: 767px) {
      .navbar-nav .nav-link {
        font-size: 0.85rem;
        margin: 0 5px;
      }

      .search-container .form-control {
        width: 150px;
      }
    }
  </style>
</head>

<body>
  <div id="mySidebar" class="sidebar">
    <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">×</a>
    <a href="/contact">CONTACT</a>
    <a href="/about">ABOUT US</a>
    <a href="/products">ALL PRODUCTS</a>
  </div>

  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <button class="openbtn me-2" onclick="openNav()">☰</button>
      <a class="navbar-brand me-3" href="/">
        <h4>TIMZO</h4>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/products?sale=true">SALE</a>
          </li>
        </ul>
        <div class="ms-auto right-section">
          <form class="search-container" action="/products" method="get">
            <input class="form-control" type="search" name="search" placeholder="What are you looking for?"
              aria-label="Search" value="<%= typeof search !== 'undefined' ? search : '' %>">
          </form>
          <a href="/user/wishlist" class="btn btn-link text-white">
            <i class="fas fa-heart"></i>
          </a>
          <a href="/user/cart" class="btn btn-link text-white position-relative">
            <i class="fas fa-shopping-cart"></i>
            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger cart-count">
              <%= typeof cartCount !=='undefined' ? cartCount : 0 %>
            </span>
          </a>
          <% if (!user) { %>
            <div class="auth-buttons">
              <a href="/login" class="btn btn-link text-white">Login / Signup</a>
            </div>
            <% } else { %>
              <div class="dropdown">
                <a class="btn btn-link dropdown-toggle user-name" href="#" role="button" data-bs-toggle="dropdown"
                  aria-expanded="false">
                  <i class="fas fa-user-circle fa-lg me-1"></i>
                  <%= user.fullname %>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                  <li><a class="dropdown-item" href="/user/account">Account</a></li>
                  <li><a class="dropdown-item" href="/user/orders">My Orders</a></li>
                  <li><a class="dropdown-item" href="/logout">Logout</a></li>
                </ul>
              </div>
              <% } %>
        </div>
      </div>
    </div>
  </nav>

  <script>
    function openNav() {
      document.getElementById("mySidebar").style.width = "250px";
    }
    function closeNav() {
      document.getElementById("mySidebar").style.width = "0";
    }
  </script>
</body>

</html>